Library for script

``
For manga script
``
pip install cloudscraper BeautifulSoup Image BytesIO ThreadPoolExecutor

``
For anime script
``
pip install asyncio yt_dlp async_playwright

And install ffmpeg to convert to mp4
If can't download directly install selenium for loading a browser to download external
pip install selenium
